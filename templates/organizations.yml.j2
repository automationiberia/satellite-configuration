---
primary_organization: "{{ primary_org }}" 

organizations:
{% for organization in info_list %}
  - name: "{{ organization.name }}"
    label: "{{ organization.label }}"
    description: "{{ organization.description }}"
{% endfor %}

all_organizations:
{% for organization in info_list %}
  - "{{ organization.name }}"
{% endfor %}

