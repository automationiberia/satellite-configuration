---
- name: Export Satellite Configuration 
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars/test_vars.yml
  tasks:
    - name: Get Subnets
      include_tasks: tasks/get_subnets.yml
